[project]
name = "wispr-unleashed"
version = "0.1.0"
description = "Continuous Wispr Flow recording with automatic Obsidian notes"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
classifiers = ["Operating System :: MacOS :: MacOS X"]
dependencies = [
    "litellm>=1.63.0",
    "pyobjc-framework-Quartz>=10.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
